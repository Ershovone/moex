// src/types/system.ts

export interface System {
  id: string;
  name: string;
  description: string;
  url: string;
}

export interface SystemCardProps {
  name: string;
  description: string;
  url: string;
}
